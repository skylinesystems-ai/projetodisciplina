export interface Training {
  name: string;
  level: string;
  duration: string;
  objective: string;
  signal: string;
}

export interface Product {
  name: string;
  price: string;
  tag: string;
  icon: string;
  accent: string;
  accent2: string;
}

export interface Article {
  category: string;
  title: string;
  excerpt: string;
  readTime: string;
}

export interface NavItem {
  label: string;
  href: string;
  icon: string;
}
